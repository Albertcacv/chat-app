import styles from './sidebar-channel.module.css'
const SidebarChannels = () => {
	return (
		<section className={styles.sidebar}> 
			Side
			<button>AÑADIR CANAL</button>
		</section>
	);
};

export default SidebarChannels;
